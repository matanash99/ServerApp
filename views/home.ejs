<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/glass.css" rel="stylesheet">
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-light navbar-glass fixed-top">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">MVC App</a>
            <div class="d-flex align-items-center">
                <span class="me-3 d-none d-sm-block">Hello, <%= user.fullName %></span>
                <form action="/auth/logout" method="POST" class="mb-0">
                    <button class="btn btn-outline-dark btn-sm rounded-pill px-3">Logout</button>
                </form>
            </div>
        </div>
    </nav>

    <div class="container d-flex align-items-center justify-content-center" style="min-height: 100vh; padding-top: 80px;">
        <div class="glass-card text-center p-5 shadow-lg">
            <div class="mb-4">
                <span style="font-size: 4rem;">👋</span>
            </div>
            <h1 class="fw-bold mb-3">Welcome Back!</h1>
            <p class="lead mb-4 text-muted">You are successfully logged in.</p>
            
            <a href="/videos" class="btn btn-primary btn-lg px-5 shadow">
                Open YouTube Studio 📺
            </a>
        </div>
    </div>

</body>
</html>