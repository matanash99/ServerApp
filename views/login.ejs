<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/glass.css" rel="stylesheet">
    <style>
        /* Add alert style directly here to be safe */
        .alert-error {
            background-color: #fee2e2;
            border: 1px solid #fca5a5;
            color: #b91c1c;
            padding: 1rem;
            border-radius: 12px;
            margin-bottom: 1.5rem;
            font-weight: bold;
        }
    </style>
</head>
<body class="d-flex align-items-center justify-content-center">

    <div class="glass-card text-center" style="width: 100%; max-width: 400px;">
        <h2 class="mb-4 fw-bold">Matan Ashkenazi</h2>
        
        <% if (locals.error) { %>
            <div class="alert-error">
                <%= error %>
            </div>
        <% } %>
        <form action="/auth/login" method="POST">
            <div class="mb-3 text-start">
                <label class="form-label ms-1">Email</label>
                <input type="email" name="email" class="form-control" placeholder="name@example.com" required>
            </div>
            <div class="mb-4 text-start">
                <label class="form-label ms-1">Password</label>
                <input type="password" name="password" class="form-control" placeholder="••••••••" required>
            </div>
            <button type="submit" class="btn btn-primary w-100 mb-3">Sign In</button>
        </form>

        <p class="mb-0 text-muted">No account? <a href="/auth/register" class="text-decoration-none fw-bold">Sign up</a></p>
    </div>

</body>
</html>